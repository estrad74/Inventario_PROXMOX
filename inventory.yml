---
# Archivo de inventario principal
all:
  hosts:
  children:
    proxmox:
      vars:
        ansible_python_interpreter: /usr/bin/python3
      plugin: community.general.proxmox
      url: https://192.168.1.52:8006
      user: root@pve
      password: pr0xm0x
      validate_certs: false
      want_facts: true
      want_hosts: true
      groupby: 
        - status
        - node
        - type
      want_proxmox_nodes_as_hosts: true
      compose:
        ansible_host: proxmox_agent_interfaces[0].ip if proxmox_agent_interfaces else proxmox_ip if proxmox_ip else ansible_host
